name: test workflow
on:
  push:
    branches:
      - 'main'

jobs:
  build:
    runs-on: alpine-latest

    steps:
      - uses: actions/checkout@v4
      - name: Use Node
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
      - run: npm ci
      - run: npm test